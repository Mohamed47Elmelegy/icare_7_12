class VideoPlayerConstants {
  // Durations
  static const controlsAutoHideDuration = Duration(seconds: 3);
  static const fadeAnimationDuration = Duration(milliseconds: 300);

  // Sizes
  static const reelAspectRatio = 9 / 16.0;
  static const playButtonSize = 70.0;
  static const playIconSize = 40.0;
  static const closeIconSize = 28.0;
  static const overlayHeight = 100.0;

  // Colors
  static const overlayOpacity = 0.7;
  static const playButtonOpacity = 0.9;
  static const shadowOpacity = 0.3;
  static const darkenOverlayOpacity = 0.3;
}
